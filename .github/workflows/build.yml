name: build
on:
  push: { branches: [main] }
  pull_request:
permissions: { contents: read }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }  # runs Gradle; foojay provisions JDK 11 for the toolchain
      - run: ./gradlew build --no-daemon
